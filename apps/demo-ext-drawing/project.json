{
  "name": "demo-ext-drawing",
  "projectType": "application",
  "tags": ["type:app", "lang:typescript"],
  "targets": {
    "dev": {
      "command": "bunx --bun vite --open /drawing.html",
      "options": { "cwd": "{projectRoot}" },
      "dependsOn": ["^build"]
    },
    "build": {
      "command": "bunx --bun vite build",
      "options": { "cwd": "{projectRoot}" },
      "dependsOn": ["^build"],
      "inputs": [
        "typescript",
        "{projectRoot}/vite.config.js",
        "{projectRoot}/src/**/*",
        "{projectRoot}/*.html",
        { "env": "VITE_BASE" },
        { "env": "VITE_IMAGES_BASE" }
      ],
      "outputs": ["{projectRoot}/dist"]
    },
    "typecheck": {
      "command": "bunx tsc --noEmit",
      "options": { "cwd": "{projectRoot}" },
      "dependsOn": ["^build"],
      "inputs": ["typescript"]
    },
    "lint": {
      "command": "bunx biome check {projectRoot}",
      "inputs": ["lintable"]
    },
    "format": {
      "command": "bunx biome format {projectRoot} --write",
      "inputs": ["lintable"]
    }
  }
}
