{
  "name": "demo-ext-save-html",
  "private": true,
  "type": "module",
  "scripts": {
    "prebuild:standalone": "bunx vite build --config vite.config.standalone.js",
    "dev": "bun run prebuild:standalone && bunx vite --open /index.html",
    "build": "bun run prebuild:standalone && bunx vite build"
  },
  "dependencies": {
    "@niivue/niivue": "workspace:*",
    "@niivue/nv-ext-save-html": "workspace:*",
    "@niivue/dev-images": "workspace:*"
  },
  "devDependencies": {
    "vite": "^6.3.2"
  }
}
