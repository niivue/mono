{
  "name": "iiif-volumetric-demo",
  "private": true,
  "type": "module",
  "scripts": {
    "gen-fixtures": "bun scripts/generate-range-poc-fixture.ts && bun scripts/generate-tile-range-poc-fixture.ts",
    "dev": "bun run gen-fixtures && bunx --bun vite --open /index.html",
    "build": "bun run gen-fixtures && bunx --bun vite build",
    "capture-screenshots": "bun scripts/capture-screenshots.ts"
  },
  "dependencies": {
    "@niivue/niivue": "workspace:*",
    "zarrita": "^0.7.3"
  },
  "devDependencies": {
    "@niivue/dev-images": "workspace:*",
    "playwright": "^1.50.0",
    "vite": "^6.3.2"
  }
}
